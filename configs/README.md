# configs

Configuration file templates for OpsMini.

`config.yaml` is the default configuration, referenced at startup via the
`-config` flag. See [`docs/build-and-deploy.md`](../docs/build-and-deploy.md)
for the full configuration reference.
