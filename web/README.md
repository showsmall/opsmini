# web

Frontend (Vue3 SPA) embedded into the binary via `embed.go` (`go:embed`).

- `index.html` — single-file prototype UI (Vue3 + ECharts);
- `embed.go` — embeds the UI into the Go binary;
- [`static/`](static/README.md) — offline frontend dependencies.
